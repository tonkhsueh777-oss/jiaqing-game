(function(root){const game=root.JQGame;const logic=game?.SkillCinematicLogic;if(!game||!logic||typeof document==='undefined')return;let overlay=null,active=false;function ensure(){if(overlay)return overlay;overlay=document.createElement('div');overlay.id='skill-cinematic';overlay.className='skill-cinematic';overlay.setAttribute('aria-hidden','true');overlay.innerHTML='<div class="skill-cinematic__backdrop"></div><div class="skill-cinematic__frame" role="dialog" aria-modal="true" aria-label="技能动画"><div class="skill-cinematic__sigil">御</div><div class="skill-cinematic__flare"></div><div class="skill-cinematic__title"></div></div>';document.body.appendChild(overlay);return overlay;}function close(){if(!overlay)return;overlay.classList.remove('is-open');overlay.setAttribute('aria-hidden','true');document.documentElement.classList.remove('skill-cinematic-active');active=false;}function playCard(card){const def=logic.getCinematic(card?.key);if(!def||active)return Promise.resolve(false);ensure();active=true;overlay.querySelector('.skill-cinematic__title').textContent=def.title;overlay.classList.add('is-open');overlay.setAttribute('aria-hidden','false');document.documentElement.classList.add('skill-cinematic-active');return new Promise(resolve=>setTimeout(()=>{close();resolve(true);},def.durationMs));}game.SkillCinematic={playCard,close,isActive:()=>active};})(globalThis);